services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vinniesbrain
    ports:
      - "5432:5432"
    volumes:
      - vinniesbrain_pgdata:/var/lib/postgresql/data

volumes:
  vinniesbrain_pgdata: